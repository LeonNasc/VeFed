# ── Federated learning backends (install one or both) ─────────────────────────
flwr>=1.8                # Flower — pip install flwr>=1.8
fedn>=0.20               # FedN  — pip install fedn

# ── LoRA fine-tuning stack ────────────────────────────────────────────────────
torch>=2.1
transformers>=4.40
peft>=0.11               # LoRA adapters via PEFT
accelerate>=0.29         # required by PEFT / transformers trainer

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.8
numpy>=1.26
scikit-learn>=1.4        # PCA for embedding scatter plots
pygame>=2.5              # town visualisation (python main.py --pygame)

# ── Experiment tracking ───────────────────────────────────────────────────────
wandb>=0.18

# ── Optional: MIMIC dataset processing ───────────────────────────────────────
# pandas>=2.0
